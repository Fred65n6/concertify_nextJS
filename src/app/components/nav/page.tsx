import React from "react";

const nav = () => {
    return (
        <div>
            This is a private component. You can only see it if you are
            authenticated.
        </div>
    );
};

export default nav;
